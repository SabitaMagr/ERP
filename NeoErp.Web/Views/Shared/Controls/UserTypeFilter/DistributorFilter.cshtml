
<select id="DistributorMultiSelect" data-bind="SP_CODE" style="width:400px;"></select>

<script>

    $(document).ready(function () {
        var autoCompleteurl = window.location.protocol + "//" + window.location.host + "/api/UserSetup/GetDistributor";
        var autoCompletedataSource = new kendo.data.DataSource({
            transport: {
                read: {
                    url: autoCompleteurl,
                    dataType: "json",
                    contentType: "application/json; charset=utf-8"
                }
            }
        });


        $('#DistributorMultiSelect').kendoMultiSelect({
            dataSource: autoCompletedataSource,
            dataTextField: "CUSTOMER_EDESC",
            dataValueField: "DISTRIBUTOR_CODE",
            height: 600,
            headerTemplate: '<div class="col-md-offset-4"><strong>..Distributor..</strong></div>',
            placeholder: "Select Distributor...",
            autoClose: false,
            dataBound: function () {
                var current = this.value();
                this._savedOld = current.slice(0);
            }
        });
    });
    //$("#DistributorMultiSelect").data("kendoMultiSelect").options.maxSelectedItems = 1;
</script>