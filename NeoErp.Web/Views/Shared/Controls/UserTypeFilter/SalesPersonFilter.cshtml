


<select id="SalesMultiSelect" data-bind="SP_CODE" style="width:400px"></select>

<script>

    $(document).ready(function () {
        debugger;
        var autoCompleteurl = window.location.protocol + "//" + window.location.host + "/api/UserSetup/GetSalePerson";
        var autoCompletedataSource = new kendo.data.DataSource({
            transport: {
                read: {
                    url: autoCompleteurl,
                    dataType: "json",
                    contentType: "application/json; charset=utf-8"
                }
            }
        });


        $('#SalesMultiSelect').kendoMultiSelect({
            dataSource: autoCompletedataSource,
            dataTextField: "EMPLOYEE_EDESC",
            dataValueField: "SP_CODE",
            height: 600,
            headerTemplate: '<div class="col-md-offset-4"><strong>..Sales Person..</strong></div>',
            placeholder: "Select Sales Person...",
            autoClose: false,
            dataBound: function () {
                var current = this.value();
                this._savedOld = current.slice(0);
            }
        });
    });
    //$("#SalesMultiSelect").data("kendoMultiSelect").options.maxSelectedItems = 1;
</script>