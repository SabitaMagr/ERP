


<select id="DistEmployeeList" data-bind="SP_CODE" style="width:100%;"></select>

<script>

    $(document).ready(function () {
        var autoCompleteurl = window.location.protocol + "//" + window.location.host + "/api/UserSetup/getUserEmployee";
        var autoCompletedataSource = new kendo.data.DataSource({
            transport: {
                read: {
                    url: autoCompleteurl,
                    dataType: "json",
                    contentType: "application/json; charset=utf-8"
                }
            }
        });


        $('#DistEmployeeList').kendoMultiSelect({
            dataSource: autoCompletedataSource,
            dataTextField: "EMPLOYEE_EDESC",
            dataValueField: "EMPLOYEE_CODE",
            height: 600,
            headerTemplate: '<div class="col-md-offset-4"><strong>..Employee..</strong></div>',
            placeholder: "Select Employee...",
            autoClose: false,
            dataBound: function () {
                var current = this.value();
                this._savedOld = current.slice(0);
            }
        });
    });
    $("#DistEmployeeList").data("kendoMultiSelect").options.maxSelectedItems = 1;
</script>