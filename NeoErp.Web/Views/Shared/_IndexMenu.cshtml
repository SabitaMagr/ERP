
@using NeoErp.Core.Helpers;
@model string

@{
    string repName = ViewBag.ReportName;
    if (repName == null)
    {
        repName = Html.Action();
    }
    string UDAction = ViewBag.UDAction;
}

@using (Html.BeginForm("", "", FormMethod.Post, new { id = "submit_udsel", name = "submit_udsel", style = "margin:0px;display:none;" }))
{
    <input type="hidden" id="UDSel" name="UDSel" />
}

<nav class="navbar navbar-default no-padding no-margin" style="margin-top:0px !important;padding-right:5px !important; border-radius:0px !important;">
    <div class="container-fluid" style="padding-left:0px;">

        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#toolbar-collapse">
                <i class="fa fa-bars"></i>
            </button>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="pull-left" id="toolbar-collapse">
            <ul class="nav navbar-nav btn-toolbar-out">
                @*@if (UDAction != null && UDAction != string.Empty)
            {
                var ActionList = UDAction.ToLower().Split(',');
                if (ActionList.Contains("create"))
                {
                        <li>
                            <a href="@Url.Action("Create",  ViewContext.GetCombinedRouteValues(new { id = Model }))" onclick="return IndexMenuAction(this, 'create');"><i class='fa fa-plus'></i> Add New</a>
                        </li>
                    }
                    if (ActionList.Contains("edit"))
                    {
                        <li>
                            <a href="@Url.Action("Edit", ViewContext.GetCombinedRouteValues(new {id = Model }))" onclick="return IndexMenuAction(this, 'Edit');"><i class='fa fa-pencil-square-o'></i> Edit</a>
                        </li>
                    }
                    if (ActionList.Contains("delete"))
                    {
                        <li>
                            <a href="@Url.Action("Delete", ViewContext.GetCombinedRouteValues(new { }))" onclick="return IndexMenuAction(this, 'delete');"><i class='fa fa-trash-o'></i> Delete</a>
                        </li>
                    }
                    if (ActionList.Contains("details"))
                    {
                        <li>
                            <a href="@Url.Action("Details", ViewContext.GetCombinedRouteValues(new { }))" onclick="return IndexMenuAction(this);"><i class='fa fa-clone'></i> Details</a>
                        </li>
                    }
                    if (ActionList.Where(wh => wh.Contains("export")).Count() > 0)
                    {
                        <li>
                            <a href="@Url.Action(ActionList.Where(wh=>wh.Contains("export")).FirstOrDefault(), ViewContext.GetCombinedRouteValues(new { }))"><i class='fa fa-file-excel-o'></i> Export</a>
                        </li>
                    }
                    if (ActionList.Where(wh => wh.Contains("report")).Count() > 0)
                    {
                        <li>
                            <a href="@Url.Action(ActionList.Where(wh=>wh.Contains("report")).FirstOrDefault(), ViewContext.GetCombinedRouteValues(new { }))"><i class='fa fa-save'></i> Run Report</a>
                        </li>
                    }
                }*@


            </ul>
            <!-- Additional Menu Items will append inside btn-toolbar -->
            <ul class="nav navbar-nav">
                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">More Options <span class="caret"></span></a>
                    <ul class="dropdown-menu btn-toolbar"></ul>
                </li>
            </ul>

            @*<form class="navbar-form navbar-right" role="search">
                    <div class="form-group">
                        <input type="text" class="form-control" placeholder="Search">
                    </div>
                    <button type="submit" class="btn btn-default">Submit</button>

                </form>*@
        </div>
        <div class="pull-right">
            @{
                if (ViewBag.filter != null)
                {

                    <form class="navbar-form navbar-right" role="search" id="gridFilters" method="post">
                        <div class="row">
                            <div class="col-xs-5 no-padding no-margin">
                                @Html.DropDownList("FilterField", (SelectList)ViewBag.filter, new { @class = "form-control", style = "width:100%" })
                            </div>
                            <div class="col-xs-7 no-padding">
                                <div class="input-group">
                                    <input type="text" name="FilterValue" class="form-control" placeholder="Search" />
                                    <div class="input-group-btn">
                                        <input type="submit" value="Filter" class="btn btn-default" />
                                    </div>
                                </div>
                            </div>

                        </div>






                    </form>

                }

            }
        </div><!-- /.navbar-collapse -->
    </div>
</nav>
